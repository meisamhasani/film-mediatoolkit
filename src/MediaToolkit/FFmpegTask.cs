namespace MediaToolkit
{
    public enum FFmpegTask
    {
        //Health check
        Check,
        GIF,
        Convert,
        GetMetaData,
        GetThumbnail,
        GenerateHLS,
        Storyboard
    }
}
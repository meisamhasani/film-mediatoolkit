namespace MediaToolkit
{
    /// -------------------------------------------------------------------------------------------------
    /// <summary>   Values that represent fmpeg tasks. </summary>
    public enum FFmpegTask
    {
        Check,
        GIF,
        Convert,
        GetMetaData,
        GetThumbnail,
        GenerateHLS
    }
}